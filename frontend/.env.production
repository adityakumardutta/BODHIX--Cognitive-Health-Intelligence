# Production API base URL — SINGLE-DEPLOYMENT (same-origin).
# The React build is embedded in the Spring Boot container, so the API is
# served from the SAME origin at /api. No cross-origin URL, no CORS.
# Contains no secrets -- this is a public path.
VITE_API_URL=/api