# Production API base URL (BODHIX backend hosted on Render).
# Loaded by `vite build` (production mode) and used by Vercel.
# Contains no secrets -- this is a public endpoint.
VITE_API_URL=https://bodhix-backend.onrender.com/api