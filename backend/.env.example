DB_HOST=localhost
DB_PORT=3306
DB_NAME=dementia_screen
DB_USER=root
DB_PASSWORD=change_me
JWT_SECRET=change_me_to_a_long_random_string_min_32_chars
JWT_EXPIRATION_MS=28800000
CORS_ALLOWED_ORIGINS=http://localhost:5173
SERVER_PORT=8080
# Real Google OAuth credentials (Google Cloud Console > OAuth client ID, "Web application").
# Authorized JavaScript origin:  http://localhost:5173
# Authorized redirect URI:      http://localhost:5173/auth/google/callback
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Email report sending (SMTP). Example: MAIL_HOST=smtp.gmail.com, MAIL_PORT=587,
# MAIL_USERNAME / MAIL_PASSWORD = SMTP account, MAIL_FROM = sender address.
MAIL_HOST=
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_FROM=
